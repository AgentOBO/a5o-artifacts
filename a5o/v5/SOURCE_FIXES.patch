--- external-candidate-as-delivered/Deployment.lean
+++ a5o/v5/Deployment.lean
@@ -29,7 +29,7 @@
   principal : Principal
   agent : Agent
   action : Action
-  at : Time
+  at_ : Time
   deriving DecidableEq, Repr
 
 /-- Constructing a value is NOT evidence that the server issued it. -/
@@ -43,7 +43,7 @@
   request_id : Nat
   agent : Agent
   action : Action
-  at : Time
+  at_ : Time
   deriving DecidableEq, Repr
 
 /-- A finite trace supplied by an adapter. Completeness is not assumed. -/
@@ -105,16 +105,16 @@
 /-- Six separate obligations. The caller must provide proofs, not declarations
 that these are axioms. No completeness of the checker is assumed. -/
 structure CheckersSound (s : EvidenceSpec) (v : Checkers) : Prop where
-  binding : ∀ q, v.binding q = true -> s.C q.principal q.agent q.action q.at
+  binding : ∀ q, v.binding q = true -> s.C q.principal q.agent q.action q.at_
   identity : ∀ p, v.identity p = true -> s.I p
   mandate : ∀ a x, v.mandate a x = true -> s.M a x
   scope : ∀ a x, v.scope a x = true -> s.S a x
-  revocation : ∀ q, v.revocation q = true -> s.R q.principal q.agent q.action q.at
-  appointment : ∀ q, v.appointment q = true -> s.Appointed q.principal q.agent q.action q.at
+  revocation : ∀ q, v.revocation q = true -> s.R q.principal q.agent q.action q.at_
+  appointment : ∀ q, v.appointment q = true -> s.Appointed q.principal q.agent q.action q.at_
 
 theorem decision_sound (s : EvidenceSpec) (v : Checkers)
     (hs : CheckersSound s v) (q : Request) (h : decision v q = true) :
-    O s.C s.I s.M s.S s.R s.Appointed q.principal q.agent q.action q.at := by
+    O s.C s.I s.M s.S s.R s.Appointed q.principal q.agent q.action q.at_ := by
   have hc := (allPass_iff (runChecks v q)).mp h
   exact ⟨hs.binding q hc.1, hs.identity q.principal hc.2.1,
     hs.mandate q.agent q.action hc.2.2.1,
@@ -124,7 +124,7 @@
 
 theorem invalid_request_denied (s : EvidenceSpec) (v : Checkers)
     (hs : CheckersSound s v) (q : Request)
-    (hBad : ¬ O s.C s.I s.M s.S s.R s.Appointed q.principal q.agent q.action q.at) :
+    (hBad : ¬ O s.C s.I s.M s.S s.R s.Appointed q.principal q.agent q.action q.at_) :
     decision v q = false := by
   cases hd : decision v q with
   | false => rfl
@@ -137,7 +137,7 @@
 
 theorem no_binding_denied (s : EvidenceSpec) (v : Checkers)
     (hs : CheckersSound s v) (q : Request)
-    (h : ¬ s.C q.principal q.agent q.action q.at) : decision v q = false :=
+    (h : ¬ s.C q.principal q.agent q.action q.at_) : decision v q = false :=
   invalid_request_denied s v hs q (fun ho => h ho.1)
 
 theorem no_mandate_denied (s : EvidenceSpec) (v : Checkers)
@@ -152,12 +152,12 @@
 
 theorem revoked_request_denied (s : EvidenceSpec) (v : Checkers)
     (hs : CheckersSound s v) (q : Request)
-    (hRevoked : ¬ s.R q.principal q.agent q.action q.at) : decision v q = false :=
+    (hRevoked : ¬ s.R q.principal q.agent q.action q.at_) : decision v q = false :=
   invalid_request_denied s v hs q (fun ho => hRevoked ho.2.2.2.2.1)
 
 theorem no_appointment_denied (s : EvidenceSpec) (v : Checkers)
     (hs : CheckersSound s v) (q : Request)
-    (h : ¬ s.Appointed q.principal q.agent q.action q.at) : decision v q = false :=
+    (h : ¬ s.Appointed q.principal q.agent q.action q.at_) : decision v q = false :=
   invalid_request_denied s v hs q (fun ho => h ho.2.2.2.2.2)
 
 /-- Listed decision flags must be linked to the actual checker outputs. -/
@@ -166,11 +166,11 @@
 
 def Matches (r : Receipt) (e : Execution) : Prop :=
   r.request.id = e.request_id ∧ r.request.agent = e.agent ∧
-  r.request.action = e.action ∧ r.request.at = e.at
+  r.request.action = e.action ∧ r.request.at_ = e.at_
 
 /-- No authorization condition appears here: a bypass remains representable. -/
 def Executes (d : Deployment) (a : Agent) (x : Action) (t : Time) : Prop :=
-  ∃ e : Execution, e ∈ d.executions ∧ e.agent = a ∧ e.action = x ∧ e.at = t
+  ∃ e : Execution, e ∈ d.executions ∧ e.agent = a ∧ e.action = x ∧ e.at_ = t
 
 /-- Cover all actions. True here broadens the domain, not a security predicate. -/
 def inAoC : Action -> Unit -> Prop := fun _ _ => True
@@ -189,7 +189,7 @@
 def gate (d : Deployment) : ADASystem Principal Agent Action Time :=
   ⟨fun p a x t => ∃ r : Receipt,
     r ∈ d.issued ∧ r.request.principal = p ∧ r.request.agent = a ∧
-    r.request.action = x ∧ r.request.at = t ∧ r.allow = true⟩
+    r.request.action = x ∧ r.request.at_ = t ∧ r.allow = true⟩
 
 theorem gate_intercepts (d : Deployment) (h : NoSideChannel d) :
     ∀ (a : Agent) (x : Action) (t : Time) (domain : Unit),
--- external-candidate-as-delivered/DeploymentTests.lean
+++ a5o/v5/DeploymentTests.lean
@@ -20,20 +20,39 @@
   identity := fun p => decide (p = "demo-principal")
   mandate := fun a x => decide (a = "demo-agent" ∧ x = "brief")
   scope := fun a x => decide (a = "demo-agent" ∧ x = "brief")
-  revocation := fun q => decide (q.at < 20)
+  revocation := fun q => decide (q.at_ < 20)
   appointment := fun q => decide
-    (q.principal = "demo-principal" ∧ q.agent = "demo-agent" ∧ q.action = "brief" ∧ 5 <= q.at)
+    (q.principal = "demo-principal" ∧ q.agent = "demo-agent" ∧ q.action = "brief" ∧ 5 <= q.at_)
 
-theorem checks_sound : CheckersSound spec checks :=
-  ⟨fun _ h => of_decide_eq_true h,
-    fun _ h => of_decide_eq_true h,
-    fun _ _ h => of_decide_eq_true h,
-    fun _ _ h => of_decide_eq_true h,
-    fun _ h => of_decide_eq_true h,
-    fun _ h => of_decide_eq_true h⟩
+theorem checks_sound : CheckersSound spec checks := by
+  refine ⟨?_, ?_, ?_, ?_, ?_, ?_⟩
+  · intro q h
+    unfold checks Checkers.binding at h
+    unfold spec EvidenceSpec.C
+    exact of_decide_eq_true h
+  · intro p h
+    unfold checks Checkers.identity at h
+    unfold spec EvidenceSpec.I
+    exact of_decide_eq_true h
+  · intro a x h
+    unfold checks Checkers.mandate at h
+    unfold spec EvidenceSpec.M
+    exact of_decide_eq_true h
+  · intro a x h
+    unfold checks Checkers.scope at h
+    unfold spec EvidenceSpec.S
+    exact of_decide_eq_true h
+  · intro q h
+    unfold checks Checkers.revocation at h
+    unfold spec EvidenceSpec.R
+    exact of_decide_eq_true h
+  · intro q h
+    unfold checks Checkers.appointment at h
+    unfold spec EvidenceSpec.Appointed
+    exact of_decide_eq_true h
 
 def allowRequest : Request := ⟨1, "demo-principal", "demo-agent", "brief", 10⟩
-def denyRequest : Request := { allowRequest with id := 2, at := 20 }
+def denyRequest : Request := { allowRequest with id := 2, at_ := 20 }
 def allowReceipt : Receipt := issue checks allowRequest
 def denyReceipt : Receipt := issue checks denyRequest
 def allowedExecution : Execution := ⟨1, "demo-agent", "brief", 10⟩
@@ -42,25 +61,48 @@
 theorem allow_decision : decision checks allowRequest = true := by decide
 theorem revocation_denies : decision checks denyRequest = false := by decide
 theorem appointment_before_start_denies :
-    decision checks { allowRequest with at := 4 } = false := by decide
+    decision checks { allowRequest with at_ := 4 } = false := by decide
 
 theorem demo_decisions : DecisionsFollowChecker checks demo := by
   intro r hr
-  have h : r = allowReceipt ∨ r = denyReceipt := by simpa [demo] using hr
+  unfold demo at hr
+  have h : r = allowReceipt ∨ r = denyReceipt := by
+    cases hr with
+    | head => exact Or.inl rfl
+    | tail _ hr' => cases hr' with
+      | head => exact Or.inr rfl
+      | tail _ hr'' => cases hr''
   cases h with
   | inl h => subst r; rfl
   | inr h => subst r; rfl
 
 theorem demo_coverage : NoSideChannel demo := by
   intro e he
-  have h : e = allowedExecution := by simpa [demo] using he
+  unfold demo at he
+  have h : e = allowedExecution := by
+    cases he with
+    | head => rfl
+    | tail _ he' => cases he'
   subst e
-  exact ⟨allowReceipt, by simp [demo], ⟨rfl, rfl, rfl, rfl⟩, by decide⟩
+  refine ⟨allowReceipt, ?_, ⟨rfl, rfl, rfl, rfl⟩, by decide⟩
+  unfold demo
+  exact List.Mem.head _
 
 theorem demo_unique_ids : UniqueReceiptIds demo := by
   intro r r' hr hr' hId
-  have h : r = allowReceipt ∨ r = denyReceipt := by simpa [demo] using hr
-  have h' : r' = allowReceipt ∨ r' = denyReceipt := by simpa [demo] using hr'
+  unfold demo at hr hr'
+  have h : r = allowReceipt ∨ r = denyReceipt := by
+    cases hr with
+    | head => exact Or.inl rfl
+    | tail _ h1 => cases h1 with
+      | head => exact Or.inr rfl
+      | tail _ h2 => cases h2
+  have h' : r' = allowReceipt ∨ r' = denyReceipt := by
+    cases hr' with
+    | head => exact Or.inl rfl
+    | tail _ h1 => cases h1 with
+      | head => exact Or.inr rfl
+      | tail _ h2 => cases h2
   cases h with
   | inl h =>
     subst r
@@ -78,7 +120,7 @@
     | inr h' => subst r'; rfl
 
 theorem allow_executed : Executes demo "demo-agent" "brief" 10 :=
-  ⟨allowedExecution, by simp [demo], rfl, rfl, rfl⟩
+  ⟨allowedExecution, by unfold demo; exact List.Mem.head _, rfl, rfl, rfl⟩
 
 theorem allow_satisfies_O :
     O spec.C spec.I spec.M spec.S spec.R spec.Appointed
@@ -102,7 +144,7 @@
 theorem deny_receipt_cannot_match_execution :
     ∀ e, e ∈ demo.executions -> ¬ Matches denyReceipt e :=
   deny_not_executed demo demo_coverage demo_unique_ids denyReceipt
-    (by simp [demo]) (by decide)
+    (by unfold demo; exact List.Mem.tail _ (List.Mem.head _)) (by decide)
 
 theorem missing_binding_denies :
     decision { checks with binding := fun _ => false } allowRequest = false := by decide
@@ -122,16 +164,22 @@
     (¬ spec.I "unknown") ∧ (¬ spec.M "demo-agent" "delete") ∧
     (¬ spec.S "demo-agent" "delete") ∧
     (¬ spec.R "demo-principal" "demo-agent" "brief" 20) ∧
-    (¬ spec.Appointed "demo-principal" "demo-agent" "brief" 4) := by decide
+    (¬ spec.Appointed "demo-principal" "demo-agent" "brief" 4) := by
+  unfold spec EvidenceSpec.C EvidenceSpec.I EvidenceSpec.M EvidenceSpec.S
+    EvidenceSpec.R EvidenceSpec.Appointed; decide
 
 theorem wrong_request_id_rejected :
-    ¬ Matches allowReceipt { allowedExecution with request_id := 99 } := by decide
+    ¬ Matches allowReceipt { allowedExecution with request_id := 99 } := by
+  unfold Matches; decide
 theorem wrong_agent_rejected :
-    ¬ Matches allowReceipt { allowedExecution with agent := "other" } := by decide
+    ¬ Matches allowReceipt { allowedExecution with agent := "other" } := by
+  unfold Matches; decide
 theorem wrong_action_rejected :
-    ¬ Matches allowReceipt { allowedExecution with action := "delete" } := by decide
+    ¬ Matches allowReceipt { allowedExecution with action := "delete" } := by
+  unfold Matches; decide
 theorem wrong_time_rejected :
-    ¬ Matches allowReceipt { allowedExecution with at := 11 } := by decide
+    ¬ Matches allowReceipt { allowedExecution with at_ := 11 } := by
+  unfold Matches; decide
 
 def phantom : Execution := ⟨99, "other", "delete", 30⟩
 theorem phantom_not_listed : ¬ (phantom ∈ demo.executions) := by decide
@@ -139,7 +187,11 @@
 theorem phantom_not_executed : ¬ Executes demo "other" "delete" 30 := by
   intro hExec
   obtain ⟨e, he, ha, _hx, _ht⟩ := hExec
-  have h : e = allowedExecution := by simpa [demo] using he
+  unfold demo at he
+  have h : e = allowedExecution := by
+    cases he with
+    | head => rfl
+    | tail _ he' => cases he'
   subst e
   exact (by decide : allowedExecution.agent ≠ "other") ha
 
@@ -147,8 +199,9 @@
 def bypass : Deployment := ⟨[], [allowedExecution]⟩
 theorem bypass_violates_coverage : ¬ NoSideChannel bypass := by
   intro h
-  obtain ⟨r, hr, _hm, _ha⟩ := h allowedExecution (by simp [bypass])
-  simpa [bypass] using hr
+  obtain ⟨r, hr, _hm, _ha⟩ := h allowedExecution (by unfold bypass; exact List.Mem.head _)
+  unfold bypass at hr
+  cases hr
 
 /-- An ALLOW flag without correct checker provenance does not suffice. -/
 def forgedReceipt : Receipt := ⟨denyRequest, true⟩
@@ -157,13 +210,20 @@
 
 theorem forged_trace_has_coverage : NoSideChannel forgedTrace := by
   intro e he
-  have h : e = forgedExecution := by simpa [forgedTrace] using he
+  unfold forgedTrace at he
+  have h : e = forgedExecution := by
+    cases he with
+    | head => rfl
+    | tail _ he' => cases he'
   subst e
-  exact ⟨forgedReceipt, by simp [forgedTrace], ⟨rfl, rfl, rfl, rfl⟩, rfl⟩
+  refine ⟨forgedReceipt, ?_, ⟨rfl, rfl, rfl, rfl⟩, rfl⟩
+  unfold forgedTrace
+  exact List.Mem.head _
 
 theorem forged_allow_fails_checker_fidelity : ¬ DecisionsFollowChecker checks forgedTrace := by
   intro h
-  have bad : (true : Bool) = false := h forgedReceipt (by simp [forgedTrace])
+  have bad : (true : Bool) = false :=
+    h forgedReceipt (by unfold forgedTrace; exact List.Mem.head _)
   exact Bool.noConfusion bad
 
 def extraActual : Agent -> Action -> Time -> Prop :=
@@ -185,26 +245,35 @@
 
 theorem lying_checks_not_sound : ¬ CheckersSound spec lyingChecks := by
   intro h
-  exact (by decide : ¬ spec.I "unknown") (h.identity "unknown" rfl)
+  exact (by unfold spec EvidenceSpec.I; decide : ¬ spec.I "unknown")
+    (h.identity "unknown" rfl)
 
 def duplicateDeny : Receipt := ⟨allowRequest, false⟩
 def ambiguous : Deployment := ⟨[allowReceipt, duplicateDeny], [allowedExecution]⟩
 
 theorem ambiguous_trace_coverage : NoSideChannel ambiguous := by
   intro e he
-  have h : e = allowedExecution := by simpa [ambiguous] using he
+  unfold ambiguous at he
+  have h : e = allowedExecution := by
+    cases he with
+    | head => rfl
+    | tail _ he' => cases he'
   subst e
-  exact ⟨allowReceipt, by simp [ambiguous], ⟨rfl, rfl, rfl, rfl⟩, by decide⟩
+  refine ⟨allowReceipt, ?_, ⟨rfl, rfl, rfl, rfl⟩, by decide⟩
+  unfold ambiguous
+  exact List.Mem.head _
 
 theorem ambiguous_ids_not_unique : ¬ UniqueReceiptIds ambiguous := by
   intro h
-  have hEq := h allowReceipt duplicateDeny (by simp [ambiguous]) (by simp [ambiguous]) rfl
+  have hEq := h allowReceipt duplicateDeny
+    (by unfold ambiguous; exact List.Mem.head _)
+    (by unfold ambiguous; exact List.Mem.tail _ (List.Mem.head _)) rfl
   have bad : (true : Bool) = false := congrArg (fun r : Receipt => r.allow) hEq
   exact Bool.noConfusion bad
 
 theorem deny_can_match_without_unique_ids :
     NoSideChannel ambiguous ∧ (¬ UniqueReceiptIds ambiguous) ∧
       Matches duplicateDeny allowedExecution :=
-  ⟨ambiguous_trace_coverage, ambiguous_ids_not_unique, by decide⟩
+  ⟨ambiguous_trace_coverage, ambiguous_ids_not_unique, by unfold Matches; decide⟩
 
 end Apertures.Tests
